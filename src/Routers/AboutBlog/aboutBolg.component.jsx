import { AboutBlogContainer } from "./aboutBolg.style";

const AboutBolg = () => {
	return (
		<AboutBlogContainer>
			<h1>持续更新中......</h1>
			<h2>
				其实写的已经差不多了，再通过React-redux什么的加一些状态，
				还有从后端请求数据，差不多就可以正式运行了。
			</h2>
		</AboutBlogContainer>
	);
};

export default AboutBolg;
